/* global p0TeamB, describe, it, expect, should */

describe('p0TeamB()', function () {
  'use strict';

  var list = [];

  it("Super filter of  ", function() {
    expect(p0TeamB([2, 4, 6])).to.deep.equal([2, 4, 6]);
  });
});
